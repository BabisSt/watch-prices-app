import PropTypes from 'prop-types';



const Layout = ({ children }) => {


    return (
        <main>
            
        </main>
);
};

Layout.propTypes = {
    children: PropTypes.node.isRequired
};

export { Layout };
